def branches = [:] 

for (int i = 0; i < 4 ; i++) {
       int index=i, branch = i+1
       stage ("stage${branch}"){ 
            branches["branch_${branch}"] = { 
                node ('maven-label'){
                    echo "hello world"
                }
            }
      }
}
